snippet 'am-avggrid' do |cmd| 
    cmd.trigger = 'am-avggrid' 
    cmd.needApplyReContentAssist = true 
    cmd.expansion = '
      <ul class="am-avg-sm-${1:1} am-avg-md-${2:2} am-avg-lg-${3:3}">
        <li>${4:内容}</li>
      </ul>
      ' 
end 

